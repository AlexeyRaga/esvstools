<Window x:Class="EventStore.VSTools.Views.Credentials.CredentialsPrompt"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EventStore Credentials" Height="170" Width="400" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Foreground="{DynamicResource VsBrush.ToolWindowText}"
        Background="{DynamicResource VsBrush.ToolWindowBackground}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ThemedControls.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Orientation="Vertical" Margin="10">
        <DockPanel Margin="5">
            <Label Width="160">Username: </Label>
            <TextBox />
        </DockPanel>
        <DockPanel Margin="5">
            <Label Width="160">Password:</Label>
            <TextBox />
        </DockPanel>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal" Margin="10,20,5,5">
            <Button Command="{Binding OkCommand}">OK</Button>
            <Button Command="{Binding CancelCommand}">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
